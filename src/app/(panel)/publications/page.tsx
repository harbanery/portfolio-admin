import PublicationDecorator from "./decorator";
import { formLayout } from "./config";

const PublicationPage = () => {
  return <PublicationDecorator formLayout={formLayout} />;
};

export default PublicationPage;
